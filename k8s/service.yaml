apiVersion: v1
kind: Service
metadata:
  name: saga-orchestrator-service
spec:
  selector:
    app: saga-orchestrator-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP

